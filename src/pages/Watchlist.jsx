import React from 'react';\n\nconst Watchlist = () => {\n  const items = [\n    { title: 'Item 1', status: 'to-watch', progress: 0 },\n    { title: 'Item 2', status: 'watching', progress: 50 },\n    { title: 'Item 3', status: 'completed', progress: 100 },\n  ];\n\n  const organizedItems = {\n    'to-watch': [],\n    'watching': [],\n    'completed': []\n  };\n\n  items.forEach(item => {\n    organizedItems[item.status].push(item);\n  });\n\n  return (\n    <div>\n      <h1>Watchlist</h1>\n      <h2>To Watch</h2>\n      <ul>\n        {organizedItems['to-watch'].map(item => (\n          <li key={item.title}>{item.title} - Progress: {item.progress}%</li>\n        ))}\n      </ul>\n      <h2>Currently Watching</h2>\n      <ul>\n        {organizedItems['watching'].map(item => (\n          <li key={item.title}>{item.title} - Progress: {item.progress}%</li>\n        ))}\n      </ul>\n      <h2>Completed</h2>\n      <ul>\n        {organizedItems['completed'].map(item => (\n          <li key={item.title}>{item.title} - Progress: {item.progress}%</li>\n        ))}\n      </ul>\n    </div>\n  );\n};\n\nexport default Watchlist;